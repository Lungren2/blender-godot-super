@tool
extends EditorPlugin

# Lifecycle-only scaffold. Transport and editor mutations are intentionally
# deferred until the shared host/bridge contracts are defined.

func _enter_tree() -> void:
    pass


func _exit_tree() -> void:
    pass
